---
title: "Hugo layout showcase"
layout: showcase
summary: "Dummy detail page for navigation, TOC, Goldmark, and shortcodes."
date: 2026-01-15
role: "Reference"
tags:
  - Hugo
  - Template
outcome: "One URL for all home cards"
aside_tidbits_label: "Tidbits"
aside_tidbits:
  - title: "Stack"
    body: |
      **Hugo** + Goldmark. No React on this demo page—just templates and CSS variables.
  - title: "Right rail"
    body: |
      These boxes come from `aside_tidbits` in front matter. Each `body` is markdown (`RenderString`).
  - title: "Layout"
    body: "Three columns on wide viewports: **Page contents** → article → tidbits. Single column stack on small screens."
markup:
  tableOfContents:
    endLevel: 4
    ordered: false
    startLevel: 2
---

## Overview

This page uses the **`showcase`** layout: a **toolbar** under the global header, a **sticky table of contents** from Hugo’s `.TableOfContents`, **body markdown** in the center, and a **right-hand tidbits** column fed from front matter.

### What you are looking at

- **Front matter** drives the document title, summary line, and **aside tidbits** (see `aside_tidbits` in this file).
- **Headings** from `##` upward populate **Page contents** in the left aside (see `[markup.tableOfContents]` in this file and site config).

## Markdown building blocks

### Lists and tasks

- Unordered item one
- Unordered item two

1. Ordered first
2. Ordered second

- [x] Task done
- [ ] Task todo

### Blockquote

> A short pull quote or citation. Hugo leaves semantics to the theme’s `.prose` styles.

### Code

Inline `code` and a fenced block:

```bash
hugo server --disableFastRender
```

```javascript
const toc = document.querySelector("#TableOfContents");
console.log(toc ? "TOC present" : "No TOC");
```

### Table

| Column A | Column B |
| -------- | -------- |
| Alpha    | 1        |
| Beta     | 2        |

### Link

[Back to home section](../../#work) (relative link from this URL) and [this site](https://work.yashraj.cc/).

### Horizontal rule

---

## Hugo shortcodes

### Other work (query)

This list is generated by a **shortcode** that calls `where` on `site.RegularPages` (excludes this showcase page):

{{< featured-work limit="4" >}}

## Mocks {#mocks}

Anchor target for **Skip to mocks** in the toolbar. Place wireframe images, embedded HTML demos, or static mocks here.

![Wide placeholder](/images/project1-thumb.png)

### Mock notes

Use this section for design notes, links to Figma, or embedded iframes when you are ready (mind CSP on Netlify).

## Appendix

**Strong**, *emphasis*, ~~strikethrough~~, and footnotes if enabled in `hugo.toml` under `[markup.goldmark]`.
